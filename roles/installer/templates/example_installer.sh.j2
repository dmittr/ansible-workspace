#!/bin/bash
# Post-run script for {{ item.name }}
# Generated by ansible installer role

set -e

echo "Running post-installation tasks for {{ item.name }}"

# Example: Validate configuration
{% if item.pkg is defined %}
echo "Package: {{ item.pkg }}"
{% endif %}

# Add your post-installation commands here
# Example:
# systemctl daemon-reload
# {{ item.name }} -t

echo "Post-run completed for {{ item.name }}"
